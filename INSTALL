This is the installation file for the rfbconnect package.

Installation is most often a matter of cd'ing to the top of the source
tree and typing'./configure', 'make', followed (as root) by 'make install'.
Those who obtained the source via git will need to do './autogen.sh' first.
